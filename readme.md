# ECG Signal Denoising

A simple Python script that reads ECG data from a CSV file and removes noise of 50Hz power line interfernce using a moving average filter, and plots the result.

The function takes the data file from a custom made ecg sensor.